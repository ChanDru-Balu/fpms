import { TextCaseDirective } from './text-case.directive';

describe('TextCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new TextCaseDirective();
    expect(directive).toBeTruthy();
  });
});
